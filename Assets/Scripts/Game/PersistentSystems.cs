using UnityEngine;

public class PersistentSystems : MonoBehaviour
{
    public static PersistentSystems Instance { get; private set; }

    private void Awake()
    {
        if (Instance != null && Instance != this)
        {
            Destroy(gameObject);
            return;
        }

        Instance = this;
        DontDestroyOnLoad(gameObject);
    }
}